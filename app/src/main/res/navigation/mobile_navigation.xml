<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_gather">

    <fragment
        android:id="@+id/navigation_gather"
        android:name="gameplay.GatherFragment"
        android:label="@string/title_gather"
        tools:layout="@layout/fragment_gather" />

    <fragment
        android:id="@+id/navigation_craft"
        android:name="gameplay.CraftFragment"
        android:label="@string/title_craft"
        tools:layout="@layout/fragment_craft" />

    <fragment
        android:id="@+id/navigation_shop"
        android:name="gameplay.ShopFragment"
        android:label="@string/title_shop"
        tools:layout="@layout/fragment_shop" />

    <fragment
        android:id="@+id/navigation_inventory"
        android:name="gameplay.InventoryFragment"
        android:label="@string/title_inventory"
        tools:layout="@layout/fragment_inventory" />
</navigation>